# Core runtime dependencies — mirrors [project.dependencies] in pyproject.toml.
# Optional extras live in pyproject.toml. Install via:
#   pip install -e ".[embeddings]"     # adds sentence-transformers + torch (+ CUDA on Linux)
#   pip install -e ".[dev]"            # dev tooling (see pyproject.toml)

mcp>=1.10.0,<2.0.0
# Upper bound: mcp 2.0.0 restructured the package and removed
# ``mcp.server.fastmcp`` (which ``src/marcus_mcp/server.py`` imports),
# so an unpinned ``>=1.10.0`` floats to 2.0.0 and breaks import/test
# collection on every branch. Cap at <2.0.0 until the 2.x import path
# is adopted deliberately.
# Transitive pins forced by mcp 1.10's streamable_http transport.
# Without anyio>=4.5 the server raises ``TypeError: 'function' object
# is not subscriptable`` (PEP 585 subscript on a non-generic function
# in anyio 3.x) and every HTTP MCP request returns 500. pydantic-
# settings and python-multipart are mcp 1.10's own minimums and pip
# warns when they are missing.
anyio>=4.5
pydantic-settings>=2.5.2
python-multipart>=0.0.9
anthropic>=0.18.0,<1.0.0
httpx>=0.25.0
aiohttp>=3.9.0
aiofiles>=0.8.0
pydantic>=2.0.0
python-dotenv>=1.0.0
rich>=13.0.0
typer>=0.9.0
numpy>=1.24.0
pandas>=2.0.0
psutil>=5.9.0
structlog>=24.0.0
jinja2>=3.1.0
# Upper bound is deliberate. mlflow ships py.typed, so a minor upgrade
# can tighten annotations and turn `mypy src/` red with no commit having
# caused it — 3.16.0 did exactly that on 2026-09-04, three weeks after
# the last green run, and it blocked every open PR at once.
# A `<4.0.0` bound would NOT have caught it: the break was inside a
# major. Raise this ceiling deliberately, after running `mypy src/`.
mlflow>=2.10.0,<3.17.0
pyyaml>=6.0.0
uvicorn>=0.25.0
python-dateutil>=2.8.0
