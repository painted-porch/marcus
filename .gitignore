node_modules/
dist/
build/
*.js
*.js.map
*.d.ts
.env
.env.local
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vscode/
.idea/
.mypy_cache/
mlruns/
.misc/
*.swp
*.swo
*~

# Runtime state — generated, never commit
data/
*.db
.coverage
htmlcov/

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Local config — only the *.example.json variants are tracked
config_marcus.json

# Generated experiment / run artifacts
logs/
mlruns/
*.egg-info/

# TypeScript — Marcus is Python; .ts files are stray experiment output
*.ts
tsconfig.json
.eslintrc.json
package.json
package-lock.json
.claude/settings.local.json
.misc/

# codex CLI local state — but keep repo-tracked skills
.codex/
!.codex/skills/
!.codex/skills/**

# AGENTS.md is auto-rendered by codex into per-experiment workdirs and
# can leak into the repo root; ignore at root to prevent accidental commits.
AGENTS.md
experiment_complete.json

# Personal advisory tooling — keep out of the public Marcus repo
skills/kaia/
skills/council/
.plans/
.env.test
.claude/settings.json
.claude/skills/

# Machine-specific workspace binding — never commit (Codex P1 on PR #725)
.marcus_workspace.json
